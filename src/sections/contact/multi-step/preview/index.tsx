import React from 'react'

import styles from './Preview.module.scss'

const PreviewStep = (): React.ReactElement => {
  return <div className={styles['preview-step-container']}>Preview step</div>
}

export default PreviewStep
