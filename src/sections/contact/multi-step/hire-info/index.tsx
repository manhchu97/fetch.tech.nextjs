import React from 'react'

import styles from './HireInfo.module.scss'

const HireInfoStep = (): React.ReactElement => {
  return <div className={styles['hire-info-container']}>Hire info step</div>
}

export default HireInfoStep
